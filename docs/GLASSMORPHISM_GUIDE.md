# 磨砂玻璃设计系统使用指南

## 📖 什么是磨砂玻璃效果（Glassmorphism）？

磨砂玻璃效果是一种现代UI设计风格，通过**背景模糊**、**半透明**和**柔和阴影**创造出类似磨砂玻璃的视觉效果。

---

## 🎨 核心原理

### 1. **关键CSS属性**

```css
.glass-card {
  /* ① 半透明背景 - 让背后内容透过来 */
  background: rgba(255, 255, 255, 0.1);
  
  /* ② 背景滤镜 - 核心！模糊背后的内容 */
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  
  /* ③ 半透明边框 - 增强玻璃感 */
  border: 1px solid rgba(255, 255, 255, 0.18);
  
  /* ④ 柔和阴影 - 增加层次感 */
  box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
}
```

### 2. **各属性详解**

| 属性 | 作用 | 参数说明 |
|------|------|----------|
| `backdrop-filter: blur()` | 模糊背后内容 | `10px`轻度, `20px`中度, `30px`重度 |
| `backdrop-filter: saturate()` | 增强饱和度 | `150%`轻微, `180%`适中, `200%`强烈 |
| `background: rgba()` | 半透明背景 | 第4个参数控制透明度 (0-1) |
| `border: rgba()` | 半透明边框 | 模拟玻璃边缘高光 |
| `box-shadow` | 柔和阴影 | 增加卡片层次感 |

---

## 🚀 快速使用

### **基础用法**

```jsx
// 深色磨砂玻璃
<div className="glass glass-dark glass-rounded-lg">
  内容
</div>

// 粉色磨砂玻璃
<div className="glass glass-pink glass-rounded-md">
  内容
</div>

// 带悬停效果
<div className="glass glass-purple glass-rounded-lg glass-hover">
  内容
</div>
```

---

## 📦 可用样式类

### **1. 基础玻璃效果**
- `.glass` - 基础磨砂玻璃
- `.glass-dark` - 深色磨砂玻璃
- `.glass-light` - 浅色磨砂玻璃

### **2. 彩色玻璃**
- `.glass-pink` - 粉紫渐变
- `.glass-green` - 绿色渐变
- `.glass-purple` - 紫色渐变
- `.glass-orange` - 橙色渐变
- `.glass-gold` - 金色渐变

### **3. 模糊程度**
- `.glass-blur-light` - 轻度模糊 (10px)
- `.glass-blur-medium` - 中度模糊 (20px)
- `.glass-blur-heavy` - 重度模糊 (30px)

### **4. 圆角**
- `.glass-rounded-sm` - 小圆角 (8px)
- `.glass-rounded-md` - 中圆角 (12px)
- `.glass-rounded-lg` - 大圆角 (16px)
- `.glass-rounded-xl` - 超大圆角 (24px)

### **5. 交互效果**
- `.glass-hover` - 悬停时提升效果

---

## 💡 使用示例

### **示例1：用户信息卡片**
```jsx
<div className="glass glass-dark glass-rounded-lg" style={{ padding: '20px' }}>
  <h2>用户信息</h2>
  <p>这是一个深色磨砂玻璃卡片</p>
</div>
```

### **示例2：彩色按钮**
```jsx
<button className="glass glass-pink glass-rounded-md glass-hover" 
        style={{ padding: '12px 24px', border: 'none', color: 'white' }}>
  开始学习
</button>
```

### **示例3：导航栏**
```jsx
<nav className="glass glass-light glass-rounded-lg" 
     style={{ padding: '16px', display: 'flex', gap: '20px' }}>
  <a href="/">首页</a>
  <a href="/study">学习</a>
  <a href="/profile">个人</a>
</nav>
```

---

## 🎯 设计建议

### ✅ **适合使用的场景**
- 卡片、面板
- 导航栏、侧边栏
- 弹窗、对话框
- 悬浮按钮
- 需要层次感的UI元素

### ❌ **不适合的场景**
- 主要内容区域（影响可读性）
- 大面积文本背景
- 需要高对比度的场景
- 性能敏感的页面（模糊消耗性能）

### 💡 **最佳实践**
1. **背景要求**：必须有背景图片或渐变色，否则看不出效果
2. **透明度控制**：`rgba()` 的透明度建议在 0.1-0.5 之间
3. **模糊度控制**：`blur()` 建议在 10px-30px 之间
4. **边框颜色**：使用半透明白色 `rgba(255, 255, 255, 0.18)`
5. **层叠使用**：可以叠加多个玻璃元素，增加层次感

---

## 🔧 自定义扩展

### **创建自己的玻璃样式**

```css
.glass-custom {
  background: rgba(你的颜色, 0.3);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.18);
  box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
  border-radius: 16px;
}
```

---

## 🌐 浏览器兼容性

| 浏览器 | 支持情况 | 备注 |
|--------|---------|------|
| Chrome 76+ | ✅ 完全支持 | |
| Safari 9+ | ✅ 完全支持 | 需要 `-webkit-` 前缀 |
| Firefox 103+ | ✅ 完全支持 | |
| Edge 79+ | ✅ 完全支持 | |
| IE | ❌ 不支持 | 降级为普通半透明 |

---

## 📚 参考资源

- [Glassmorphism CSS Generator](https://hype4.academy/tools/glassmorphism-generator)
- [CSS backdrop-filter - MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/backdrop-filter)
- [Glassmorphism in UI Design](https://uxdesign.cc/glassmorphism-in-user-interfaces-1f39bb1308c9)

---

## 🎨 在本项目中的应用

本项目已将磨砂玻璃效果应用于：
- ✅ 首页所有卡片
- ✅ 用户信息面板
- ✅ 签到卡片
- ✅ 学习模块卡片
- ✅ 排行榜

你可以在任何新组件中直接使用这些样式类！
